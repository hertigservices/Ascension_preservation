const cache=new Map();let latest=0;
const norm=s=>String(s).normalize('NFKD').replace(/\p{M}/gu,'').toLowerCase().replaceAll('ß','ss');
const words=s=>norm(s).match(/[\p{L}\p{N}]+/gu)||[];
async function load(path){if(cache.has(path)){const v=cache.get(path);cache.delete(path);cache.set(path,v);return v;}const r=await fetch(path);if(!r.ok)throw Error(`Catalog part unavailable (${r.status}). Refresh to load the latest snapshot.`);const a=await r.arrayBuffer();const b=new Uint8Array(a);const v= b[0]===31&&b[1]===139?await new Response(new Blob([a]).stream().pipeThrough(new DecompressionStream('gzip'))).json():JSON.parse(new TextDecoder().decode(a));cache.set(path,v);while(cache.size>12)cache.delete(cache.keys().next().value);return v;}
onmessage=async({data:d})=>{const job=++latest;if(d.cancel)return;try{const m=d.manifest,q=d.q.trim(),ts=words(q).filter(x=>x.length>=2),numeric=/^-?\d+$/.test(q);let key;if(numeric)key='id:'+q.slice(0,2);else if(ts.length)key=ts.map(t=>'name:'+t.slice(0,2)).sort((a,b)=>(m.search[a]?.count||0)-(m.search[b]?.count||0))[0];else key='browse:'+(d.kind||'item');const entry=m.search[key];if(!entry){postMessage({id:d.id,rows:[],total:0});return;}let total=0,results=[];const start=d.page*50,end=start+50;const fastBrowse=!q&&!d.source&&!d.mode;
for(let i=0;i<entry.parts.length;i++){const rows=await load((d.dataBase||'')+entry.parts[i]);if(job!==latest)return;if(fastBrowse){const lo=Math.max(0,start-total),hi=Math.min(rows.length,end-total);if(hi>lo)results.push(...rows.slice(lo,hi));total+=rows.length;if(total>=end){postMessage({id:d.id,rows:results,total:entry.count});return;}continue;}for(const r of rows){if(d.kind&&r[2]!==d.kind||d.source&&r[4]!==d.source||d.mode&&!r[3].split(',').map(s=>s.trim()).includes(d.mode))continue;if(numeric&&r[5]!==q)continue;if(!numeric&&ts.length&&!ts.every(t=>words(r[1]).some(w=>w.startsWith(t))))continue;if(total>=start&&total<end)results.push(r);total++;}postMessage({id:d.id,progress:i+1,parts:entry.parts.length});}postMessage({id:d.id,rows:results,total:fastBrowse?entry.count:total});}catch(e){postMessage({id:d.id,error:e.message});}};
